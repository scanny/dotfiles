return {
    settings = {
        python = {
            analysis = {
                typeCheckingMode = "strict"
                -- typeCheckingMode = "basic"
                -- typeCheckingMode = "off"
          }
        }
    }
}
